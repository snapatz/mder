# Ship Windows first

The first version will target Windows because the core workflow is opening `.md` files through the operating system's default-app association, and this repo is being developed on Windows. Tauri may support other platforms later, but v1 decisions should optimize the Windows install, open-file, and editing experience before spending time on macOS or Linux packaging.
