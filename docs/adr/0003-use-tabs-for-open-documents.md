# Use tabs for open documents

The app will open multiple Markdown Documents as tabs in one main window, including files launched from Explorer while the app is already running. This makes default-app opening feel collected and workspace-like instead of creating one window per file.
